{{- if .Values.redis.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "icebreaker.fullname" . }}-redis
  labels:
    {{- include "icebreaker.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.redis.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "icebreaker.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: redis
{{- end }}
